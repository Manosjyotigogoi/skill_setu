# Base URL for the Skill-Setu backend API.
# In dev, leave this unset — vite.config.js proxies '/api' to localhost:5000.
# In production, set this to the absolute URL of your deployed backend, e.g.
#   VITE_API_URL=https://api.skillsetu.gov.in/api
VITE_API_URL=/api
